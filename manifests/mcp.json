{
  "$comment": "Installed globally by bootstrap.sh --mcp, so claude/mcp/ starts a binary rather than resolving npx every session.",
  "$why": "Nothing loads by default; a server costs tool definitions in every prompt, not disk. Pass --mcp-config and --strict-mcp-config per session.",
  "packages": {
    "@playwright/mcp": {
      "version": "0.0.78",
      "bin": "playwright-mcp",
      "category": "web",
      "why": "browser automation as tool calls; its tools error until `npx playwright install chromium`"
    },
    "firebase-tools": {
      "version": "15.25.1",
      "bin": "firebase",
      "category": "cloud",
      "why": "Firestore, Auth, Hosting, deploys. `--dir .` resolves the project from the cwd"
    }
  }
}
